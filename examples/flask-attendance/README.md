# Flask attendance

```bash
cd examples/flask-attendance
pip install -e ../../python flask
python app.py
```

Open http://localhost:3848

1. **Issue code** → **Scan** (allowed) → **Scan** again (blocked)

A [LogicLitz](https://logiclitz.org) example for admitiq.
